import Box from "@mui/material/Box";
import Typography from "@mui/material/Typography";

function Nav(props) {
  return (
    <Box sx={{ backgroundColor: "#cd452b", color: "#fff", paddingLeft: 1 }}>
      <Typography variant="h3">Timer</Typography>
    </Box>
  );
}

export default Nav;
